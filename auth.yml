spring:
  datasource:
    url: jdbc:postgresql://db-auth:5432/users-auth
    username: admin
    password: admin1234@
  flyway:
    baseline-on-migrate: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

api:
  security:
    token:
      secret: ${JWT_SECRET}
